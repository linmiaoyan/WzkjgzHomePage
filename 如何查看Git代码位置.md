# 如何查看已上传的 Git 代码位置

## 🚀 快速查看方法

### 方法1：使用脚本（最简单）

**双击运行：** `查看Git仓库位置.bat`

这个脚本会显示：
- ✅ 所有配置的远程仓库URL
- ✅ 如何访问这些仓库
- ✅ 最近的提交历史
- ✅ 本地和远程的同步状态

---

## 📋 手动查看方法

### 查看远程仓库配置

```bash
# 进入项目目录
cd D:\OneDrive\09教育技术处\WzkjHomepage

# 查看所有远程仓库
git remote -v

# 查看特定远程仓库的URL
git remote get-url origin
# 或
git remote get-url server
```

### 查看提交历史

```bash
# 查看最近10次提交
git log --oneline -10

# 查看详细提交信息
git log

# 查看某个远程仓库的提交
git log origin/main
```

---

## 🔍 不同仓库类型的访问方式

### 1. 在线仓库（GitHub/Gitee/GitLab）

**查看方式：**
- 直接在浏览器中访问显示的URL
- 例如：`https://github.com/username/WzkjHomepage`
- 可以在网页上查看所有代码、提交历史、分支等

**示例：**
```bash
# 如果配置的是GitHub
git remote get-url origin
# 输出：https://github.com/username/WzkjHomepage.git

# 浏览器访问：
# https://github.com/username/WzkjHomepage
```

---

### 2. 服务器Git仓库

**查看方式：**
- SSH连接到服务器
- 进入配置的Git仓库路径或工作目录

**示例：**
```bash
# 查看远程仓库URL
git remote get-url server
# 输出：root@192.168.1.100:/var/git/WzkjHomepage.git

# 这意味着：
# - 服务器：192.168.1.100
# - Git仓库位置：/var/git/WzkjHomepage.git
# - 工作目录：通常在 /var/www/WzkjHomepage
```

**在服务器上查看：**
```bash
# SSH连接
ssh root@192.168.1.100

# 查看Git仓库
cd /var/git/WzkjHomepage.git
ls -la

# 查看工作目录（实际代码）
cd /var/www/WzkjHomepage
ls -la
```

---

## 📍 代码实际存储位置

### 本地代码位置

```
D:\OneDrive\09教育技术处\WzkjHomepage
```

这是你的**本地仓库**，所有代码都在这里。

### 远程代码位置

根据你配置的远程仓库类型，代码可能存储在：

**1. 在线仓库（GitHub/Gitee）**
- 位置：GitHub/Gitee服务器
- 访问：浏览器访问仓库URL
- 查看：在网页上直接查看代码

**2. 服务器Git仓库**
- Git仓库位置：`/var/git/WzkjHomepage.git`（裸仓库）
- 工作目录：`/var/www/WzkjHomepage`（实际运行的代码）
- 访问：SSH连接到服务器查看

---

## 🔍 完整查看流程

### 步骤1：查看远程仓库配置

```bash
git remote -v
```

**输出示例：**
```
origin  https://github.com/username/WzkjHomepage.git (fetch)
origin  https://github.com/username/WzkjHomepage.git (push)
server  root@192.168.1.100:/var/git/WzkjHomepage.git (fetch)
server  root@192.168.1.100:/var/git/WzkjHomepage.git (push)
```

### 步骤2：根据仓库类型访问

**如果是GitHub/Gitee：**
- 浏览器访问：`https://github.com/username/WzkjHomepage`
- 或：`https://gitee.com/username/WzkjHomepage`

**如果是服务器仓库：**
```bash
# SSH连接
ssh root@192.168.1.100

# 查看工作目录（实际代码）
cd /var/www/WzkjHomepage
ls -la

# 查看文件内容
cat main.py
```

### 步骤3：查看提交历史

```bash
# 查看推送的提交
git log --oneline -10

# 查看远程仓库的提交
git fetch origin
git log origin/main --oneline -10
```

---

## 🎯 快速检查命令

### 一键查看所有信息

```bash
# 查看远程仓库
echo "=== 远程仓库 ==="
git remote -v

# 查看提交历史
echo "=== 最近提交 ==="
git log --oneline -5

# 查看同步状态
echo "=== 同步状态 ==="
git status

# 查看分支
echo "=== 分支信息 ==="
git branch -a
```

---

## 📝 常见问题

### Q1: 我推送了代码，但不知道推送到哪里了？

**解决方案：**
```bash
# 查看远程仓库配置
git remote -v

# 查看最近推送的记录
git log --oneline -5
```

### Q2: 如何确认代码已经推送到服务器？

**解决方案：**
```bash
# SSH到服务器
ssh root@your-server-ip

# 进入项目目录
cd /var/www/WzkjHomepage

# 查看最新提交
git log --oneline -1

# 查看文件修改时间
ls -l main.py
```

### Q3: 如何查看在线仓库的代码？

**解决方案：**
- 如果使用GitHub：访问 `https://github.com/username/repo`
- 如果使用Gitee：访问 `https://gitee.com/username/repo`
- 如果使用GitLab：访问 `https://gitlab.com/username/repo`

---

## 💡 推荐工具

### Windows 图形化工具

1. **GitHub Desktop** - 如果使用GitHub
2. **SourceTree** - 支持多种Git服务
3. **VS Code** - 内置Git支持，可以查看提交历史

### 命令行工具

- `git remote -v` - 查看远程仓库
- `git log` - 查看提交历史
- `git show` - 查看具体提交内容
- `git diff` - 查看差异

---

## 🎁 快速脚本

运行 `查看Git仓库位置.bat` 可以一次性查看：
- 所有远程仓库URL
- 如何访问这些仓库
- 提交历史
- 同步状态

